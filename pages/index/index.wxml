<view class="container">
  <view class="title">预约课程</view>
  <block wx:for="{{courses}}" wx:key="id">
    <view class="course-item">
      <image class="course-image" src="{{item.course_image}}" />
      <text class="course-name">{{item.course_name}}</text>
    </view>
  </block>
</view>
